#include "main.h"

/**
 * mul - multiplies tow integers
 * @a: to check the first integer
 * @b: to check the scanod integer
 * Return: mult
 */

int mul(int a, int b)
{
	int mult;

	mult = (a * b);

	return (mult);
}
